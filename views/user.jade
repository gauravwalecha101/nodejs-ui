doctype html
html
  head
    title Page Title
    style.
      header {
      background-color: #666;
      padding: 2px;
      text-align: center;
      font-size: 15px;
      color: white;
      }
      .topnav {
      overflow: hidden;
      background-color: #e9e9e9;
      }
      .topnav a {
      float: left;
      display: block;
      color: black;
      text-align: center;
      padding: 14px 16px;
      text-decoration: none;
      font-size: 17px;
      }
      .topnav a:hover {
      background-color: #ddd;
      color: black;
      }
      .topnav a.active {
      background-color: #2196F3;
      color: white;
      }
  body
    h1.header Bank of Future
    .topnav
      a.active(onclick='home()', href='/user') Home
      a(onclick='findBranches()', href='/findbranches') Find Branches
    // <button type="button" onclick="home()">Home</button>
    // <button type="button" onclick="findBranches()">Find Branches</button>
    script.
      const findBranches = async () => {
      window.location.href = '/findbranches'
      }
      const home = async () => {
      window.location.href = '/user'
      }
